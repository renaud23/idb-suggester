language: node_js
node_js:
 - "stable"
script:
  - npm test
  - npm run build-storybook
deploy:
  provider: surge
  skip_cleanup: true
  domain: idb-suggester.surge.sh
  project: ./lib/
on:
  branch: master
